# Archived C++ Correctness Tests

These files are kept as historical references for the original C++ numerical
correctness checks. They are intentionally not included by the default
`tests/CMakeLists.txt`.

The active correctness coverage now lives in `tests/python/` so it exercises the
same public surface used by PyTorch dispatcher integration.
